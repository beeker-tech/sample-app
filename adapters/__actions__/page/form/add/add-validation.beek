The language is typescript.

Add the following imports:
- import * as yup from 'yup';
- import { Schema } from 'yup';
- import { FormValues } from './values';

Add an exported empty constant named validationSchema with Schema<FormValues>.

Make it pretty with prettier.